services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: securedoc
      POSTGRES_USER: securedoc
      POSTGRES_PASSWORD: securedoc-local-only
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
